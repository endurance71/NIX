name: Preview (internal)

on:
  pull_request: {}

jobs:
  build_ios_preview:
    name: Build iOS preview
    type: build
    params:
      platform: ios
      profile: preview

  build_android_preview:
    name: Build Android preview
    type: build
    params:
      platform: android
      profile: preview
