diff --git a/node_modules/expo-camera/ios/Current/CameraSessionManager.swift b/node_modules/expo-camera/ios/Current/CameraSessionManager.swift
index 2b68d31..46cce7b 100644
--- a/node_modules/expo-camera/ios/Current/CameraSessionManager.swift
+++ b/node_modules/expo-camera/ios/Current/CameraSessionManager.swift
@@ -25,6 +25,9 @@ protocol CameraSessionManagerDelegate: AnyObject {
 }
 
 class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
+  private let videoTorchInitialLevel: Float = 0.12
+  private let videoTorchTargetLevel: Float = 0.55
+
   weak var delegate: CameraSessionManagerDelegate?
 
   let session = AVCaptureSession()
@@ -82,6 +85,7 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
     guard let delegate else {
       return
     }
+    logTorchDiagnostic(event: "updateDevice-begin", reason: "preset=\(delegate.presetCamera.rawValue)")
 
     let lenses = delegate.presetCamera == .back
     ? deviceDiscovery.backCameraLenses
@@ -108,6 +112,7 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
     guard let delegate else {
       return
     }
+    logTorchDiagnostic(event: "updateCameraIsActive", reason: "active=\(delegate.active)")
     if delegate.active {
       if !self.session.isRunning {
         self.session.startRunning()
@@ -121,8 +126,8 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
     guard let delegate else {
       return
     }
+    logTorchDiagnostic(event: "setCameraMode-begin", reason: "mode=\(delegate.mode.rawValue)")
     self.session.beginConfiguration()
-    defer { self.session.commitConfiguration() }
     if delegate.mode == .video {
       if self.videoFileOutput == nil {
         self.setupMovieFileCapture(withSessionConfiguration: false)
@@ -136,6 +141,16 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
         self.configureResponsiveCapture(photoOutput)
       }
     }
+    self.session.commitConfiguration()
+    logTorchDiagnostic(event: "setCameraMode-commit", reason: "mode=\(delegate.mode.rawValue)")
+    if delegate.active && !self.session.isRunning {
+      logTorchDiagnostic(event: "setCameraMode-restart-session", reason: "mode=\(delegate.mode.rawValue)")
+      self.session.startRunning()
+    }
+    DispatchQueue.main.async { [weak delegate] in
+      delegate?.onCameraReady()
+    }
+    self.enableTorchForVideoRecording(reason: "setCameraMode")
   }
 
   func updateSessionAudioIsMuted(withSessionConfiguration: Bool = true) {
@@ -177,20 +192,87 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
     }
   }
 
-  func enableTorch() {
-    guard let delegate, let device = captureDeviceInput?.device, device.hasTorch else {
+  func enableTorch(reason: String = "prop") {
+    logTorchDiagnostic(event: "enableTorch-prop", reason: reason)
+    setTorchEnabled(delegate?.torchEnabled ?? false, reason: reason)
+  }
+
+  private func setTorchEnabled(_ enabled: Bool, reason: String, level: Float? = nil) {
+    guard delegate != nil, let device = captureDeviceInput?.device, device.hasTorch else {
+      logTorchDiagnostic(event: "setTorchEnabled-skip-no-device-or-torch", reason: "\(reason) enabled=\(enabled)", device: captureDeviceInput?.device)
       return
     }
 
+    let requestedLevel = level.map { String($0) } ?? "max"
+    logTorchDiagnostic(event: "setTorchEnabled-before-lock", reason: "\(reason) enabled=\(enabled) requestedLevel=\(requestedLevel)", device: device)
     do {
       try device.lockForConfiguration()
       defer { device.unlockForConfiguration() }
       if device.isTorchModeSupported(.on) {
-        device.torchMode = delegate.torchEnabled ? .on : .off
+        if enabled {
+          let torchLevel = min(level ?? AVCaptureDevice.maxAvailableTorchLevel, AVCaptureDevice.maxAvailableTorchLevel)
+          logTorchDiagnostic(event: "setTorchModeOn-before", reason: "\(reason) level=\(torchLevel)", device: device)
+          try device.setTorchModeOn(level: max(torchLevel, 0.01))
+        } else {
+          logTorchDiagnostic(event: "setTorchModeOff-before", reason: reason, device: device)
+          device.torchMode = .off
+        }
+      } else {
+        logTorchDiagnostic(event: "setTorchEnabled-skip-unsupported", reason: "\(reason) enabled=\(enabled)", device: device)
       }
+      logTorchDiagnostic(event: "setTorchEnabled-after", reason: "\(reason) enabled=\(enabled)", device: device)
     } catch {
       log.info("\(#function): \(error.localizedDescription)")
+      logTorchDiagnostic(event: "setTorchEnabled-failed", reason: "\(reason) error=\(error.localizedDescription)", device: device)
+    }
+  }
+
+  func enableTorchForVideoRecording(reason: String) {
+    logTorchDiagnostic(event: "enableTorchForVideoRecording-begin", reason: reason)
+    guard let delegate, delegate.mode == .video, delegate.torchEnabled else {
+      logTorchDiagnostic(event: "enableTorchForVideoRecording-disable-or-skip", reason: reason)
+      setTorchEnabled(false, reason: reason)
+      return
     }
+    logTorchDiagnostic(event: "enableTorchForVideoRecording-soft-start-dispatch", reason: reason)
+    setTorchEnabled(true, reason: "\(reason)-soft-start", level: videoTorchInitialLevel)
+    logTorchDiagnostic(event: "enableTorchForVideoRecording-soft-target-scheduled", reason: reason)
+    delegate.sessionQueue.asyncAfter(deadline: .now() + 0.18) { [weak self, weak delegate] in
+      guard let self, let delegate, delegate.mode == .video, delegate.torchEnabled else {
+        self?.logTorchDiagnostic(event: "enableTorchForVideoRecording-soft-target-skip", reason: reason)
+        return
+      }
+      self.logTorchDiagnostic(event: "enableTorchForVideoRecording-soft-target-fire", reason: reason)
+      self.setTorchEnabled(true, reason: "\(reason)-soft-target", level: self.videoTorchTargetLevel)
+    }
+  }
+
+  func logVideoTorchIntent(reason: String) {
+    logTorchDiagnostic(event: "videoTorchIntent", reason: reason, device: captureDeviceInput?.device)
+  }
+
+  private func logTorchDiagnostic(event: String, reason: String = "nil", device: AVCaptureDevice? = nil) {
+    #if DEBUG
+    let activeDevice = device ?? captureDeviceInput?.device
+    let deviceName = activeDevice?.localizedName ?? "nil"
+    let deviceType = activeDevice?.deviceType.rawValue ?? "nil"
+    let hasTorch = activeDevice?.hasTorch ?? false
+    let torchMode = activeDevice?.torchMode.rawValue ?? -1
+    let torchLevel = activeDevice?.torchLevel ?? -1
+    let requested = delegate?.torchEnabled ?? false
+    let mode = delegate?.mode.rawValue ?? "nil"
+    let active = delegate?.active ?? false
+    let sessionRunning = session.isRunning
+    let hasVideoOutput = videoFileOutput != nil
+    let hasPhotoOutput = photoOutput != nil
+    let currentInputName = captureDeviceInput?.device.localizedName ?? "nil"
+    let message =
+      "[ExpoCameraTorch] event=\(event) reason=\(reason) t=\(CFAbsoluteTimeGetCurrent()) torchRequested=\(requested) mode=\(mode) active=\(active) sessionRunning=\(sessionRunning) hasTorch=\(hasTorch) torchMode=\(torchMode) torchLevel=\(torchLevel) device=\(deviceName) deviceType=\(deviceType) currentInput=\(currentInputName) hasVideoOutput=\(hasVideoOutput) hasPhotoOutput=\(hasPhotoOutput)"
+    NSLog("%@", message)
+    log.info(
+      "\(message)"
+    )
+    #endif
   }
 
   func setFocusMode() {
@@ -241,23 +323,29 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
   }
 
   func setupMovieFileCapture(withSessionConfiguration: Bool = true) {
+    logTorchDiagnostic(event: "setupMovieFileCapture-begin", reason: "withSessionConfiguration=\(withSessionConfiguration)")
     let output = AVCaptureMovieFileOutput()
     if withSessionConfiguration {
       session.beginConfiguration()
     }
-    defer {
-      if withSessionConfiguration {
-        session.commitConfiguration()
-      }
-    }
     if session.canAddOutput(output) {
       session.addOutput(output)
       videoFileOutput = output
+      logTorchDiagnostic(event: "setupMovieFileCapture-added", reason: "withSessionConfiguration=\(withSessionConfiguration)")
+    } else {
+      logTorchDiagnostic(event: "setupMovieFileCapture-cannot-add", reason: "withSessionConfiguration=\(withSessionConfiguration)")
+    }
+    if withSessionConfiguration {
+      session.commitConfiguration()
+      logTorchDiagnostic(event: "setupMovieFileCapture-commit", reason: "withSessionConfiguration=\(withSessionConfiguration)")
+      enableTorchForVideoRecording(reason: "setupMovieFileCapture")
     }
   }
 
   func cleanupMovieFileCapture(withSessionConfiguration: Bool = true) {
+    logTorchDiagnostic(event: "cleanupMovieFileCapture-begin", reason: "withSessionConfiguration=\(withSessionConfiguration)")
     guard let videoFileOutput else {
+      logTorchDiagnostic(event: "cleanupMovieFileCapture-no-output", reason: "withSessionConfiguration=\(withSessionConfiguration)")
       return
     }
     if withSessionConfiguration {
@@ -271,10 +359,12 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
     if session.outputs.contains(videoFileOutput) {
       session.removeOutput(videoFileOutput)
       self.videoFileOutput = nil
+      logTorchDiagnostic(event: "cleanupMovieFileCapture-removed", reason: "withSessionConfiguration=\(withSessionConfiguration)")
     }
   }
 
   func stopSession() {
+    logTorchDiagnostic(event: "stopSession-begin")
     guard hasAvailableCameraDevice else {
       return
     }
@@ -294,6 +384,7 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
     if session.isRunning {
       session.stopRunning()
     }
+    logTorchDiagnostic(event: "stopSession-end")
   }
 
   func addErrorNotification() {
@@ -331,6 +422,7 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
     guard let delegate else {
       return
     }
+    logTorchDiagnostic(event: "addDevice-begin", reason: "newDevice=\(device.localizedName)", device: device)
 
     if captureDeviceInput?.device.uniqueID == device.uniqueID {
       return
@@ -341,6 +433,10 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
       session.commitConfiguration()
       delegate.emitAvailableLenses()
       delegate.configurePreviewRotation()
+      logTorchDiagnostic(event: "addDevice-commit", reason: "newDevice=\(device.localizedName)", device: captureDeviceInput?.device)
+      if delegate.torchEnabled {
+        enableTorchForVideoRecording(reason: "addDevice")
+      }
     }
     if let captureDeviceInput {
       session.removeInput(captureDeviceInput)
@@ -352,8 +448,12 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
         session.addInput(deviceInput)
         captureDeviceInput = deviceInput
         updateZoom()
+        logTorchDiagnostic(event: "addDevice-added-input", reason: "newDevice=\(device.localizedName)", device: device)
+      } else {
+        logTorchDiagnostic(event: "addDevice-cannot-add-input", reason: "newDevice=\(device.localizedName)", device: device)
       }
     } catch {
+      logTorchDiagnostic(event: "addDevice-error", reason: error.localizedDescription, device: device)
       delegate.onMountError(["message": "Camera could not be started - \(error.localizedDescription)"])
     }
   }
@@ -379,6 +479,7 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
   }
 
   private func startSession() {
+    logTorchDiagnostic(event: "startSession-begin")
     guard hasAvailableCameraDevice else {
       return
     }
@@ -411,9 +512,14 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
     configureResponsiveCapture(photoOutput)
 
     session.commitConfiguration()
+    logTorchDiagnostic(event: "startSession-commit")
     addErrorNotification()
     delegate.configurePreviewRotation()
     delegate.barcodeScanner?.maybeStartBarcodeScanning()
-    enableTorch()
+
+    DispatchQueue.main.async { [weak delegate] in
+      delegate?.onCameraReady()
+    }
+    enableTorchForVideoRecording(reason: "startSession")
   }
 }
diff --git a/node_modules/expo-camera/ios/Current/CameraSessionManager.swift.orig b/node_modules/expo-camera/ios/Current/CameraSessionManager.swift.orig
new file mode 100644
index 0000000..2b68d31
--- /dev/null
+++ b/node_modules/expo-camera/ios/Current/CameraSessionManager.swift.orig
@@ -0,0 +1,419 @@
+import UIKit
+@preconcurrency import AVFoundation
+import ExpoModulesCore
+
+protocol CameraSessionManagerDelegate: AnyObject {
+  var sessionQueue: DispatchQueue { get }
+  var videoQuality: VideoQuality { get }
+  var mode: CameraMode { get }
+  var pictureSize: PictureSize { get }
+  var isMuted: Bool { get }
+  var active: Bool { get }
+  var presetCamera: AVCaptureDevice.Position { get }
+  var selectedLens: String? { get }
+  var torchEnabled: Bool { get }
+  var autoFocus: AVCaptureDevice.FocusMode { get }
+  var zoom: CGFloat { get }
+  var onMountError: EventDispatcher { get }
+  var onCameraReady: EventDispatcher { get }
+  var permissionsManager: EXPermissionsInterface? { get }
+  var appContext: AppContext? { get }
+  var barcodeScanner: BarcodeScanner? { get }
+
+  func emitAvailableLenses()
+  func configurePreviewRotation()
+}
+
+class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
+  weak var delegate: CameraSessionManagerDelegate?
+
+  let session = AVCaptureSession()
+  private let deviceDiscovery = DeviceDiscovery()
+
+  private var captureDeviceInput: AVCaptureDeviceInput?
+  private var photoOutput: AVCapturePhotoOutput?
+  private var videoFileOutput: AVCaptureMovieFileOutput?
+  private var runtimeErrorTask: Task<Void, Never>?
+
+  init(delegate: CameraSessionManagerDelegate) {
+    self.delegate = delegate
+    super.init()
+    deviceDiscovery.delegate = self
+  }
+
+  func initializeCaptureSessionInput() {
+    guard let delegate else {
+      return
+    }
+    delegate.sessionQueue.async {
+      self.updateDevice()
+      self.startSession()
+    }
+  }
+
+  func updateSessionPreset(preset: AVCaptureSession.Preset, withSessionConfiguration: Bool = true) {
+    guard hasAvailableCameraDevice else {
+      return
+    }
+    if session.canSetSessionPreset(preset) {
+      if session.sessionPreset != preset {
+        if withSessionConfiguration {
+          session.beginConfiguration()
+        }
+        session.sessionPreset = preset
+        if withSessionConfiguration {
+          session.commitConfiguration()
+        }
+      }
+    } else {
+      if session.sessionPreset != .high {
+        if withSessionConfiguration {
+          session.beginConfiguration()
+        }
+        session.sessionPreset = .high
+        if withSessionConfiguration {
+          session.commitConfiguration()
+        }
+      }
+    }
+  }
+
+  func updateDevice() {
+    guard let delegate else {
+      return
+    }
+
+    let lenses = delegate.presetCamera == .back
+    ? deviceDiscovery.backCameraLenses
+    : deviceDiscovery.frontCameraLenses
+
+    let selectedDevice = lenses.first {
+      $0.localizedName == delegate.selectedLens
+    }
+
+    if let selectedDevice {
+      addDevice(selectedDevice)
+    } else {
+      let device = delegate.presetCamera == .back
+      ? deviceDiscovery.defaultBackCamera
+      : deviceDiscovery.defaultFrontCamera
+
+      if let device {
+        addDevice(device)
+      }
+    }
+  }
+
+  func updateCameraIsActive() {
+    guard let delegate else {
+      return
+    }
+    if delegate.active {
+      if !self.session.isRunning {
+        self.session.startRunning()
+      }
+    } else {
+      self.session.stopRunning()
+    }
+  }
+
+  func setCameraMode() {
+    guard let delegate else {
+      return
+    }
+    self.session.beginConfiguration()
+    defer { self.session.commitConfiguration() }
+    if delegate.mode == .video {
+      if self.videoFileOutput == nil {
+        self.setupMovieFileCapture(withSessionConfiguration: false)
+      }
+      self.updateSessionAudioIsMuted(withSessionConfiguration: false)
+      self.updateSessionPreset(preset: delegate.videoQuality.toPreset(), withSessionConfiguration: false)
+    } else {
+      self.cleanupMovieFileCapture(withSessionConfiguration: false)
+      self.updateSessionPreset(preset: delegate.pictureSize.toCapturePreset(), withSessionConfiguration: false)
+      if let photoOutput {
+        self.configureResponsiveCapture(photoOutput)
+      }
+    }
+  }
+
+  func updateSessionAudioIsMuted(withSessionConfiguration: Bool = true) {
+    guard let delegate else {
+      return
+    }
+
+    if withSessionConfiguration {
+      session.beginConfiguration()
+    }
+    defer {
+      if withSessionConfiguration {
+        session.commitConfiguration()
+      }
+    }
+
+    if delegate.isMuted {
+      for input in session.inputs {
+        if let deviceInput = input as? AVCaptureDeviceInput {
+          if deviceInput.device.hasMediaType(.audio) {
+            session.removeInput(input)
+            return
+          }
+        }
+      }
+    }
+
+    if !delegate.isMuted && delegate.mode == .video {
+      if let audioCapturedevice = AVCaptureDevice.default(for: .audio) {
+        do {
+          let audioDeviceInput = try AVCaptureDeviceInput(device: audioCapturedevice)
+          if session.canAddInput(audioDeviceInput) {
+            session.addInput(audioDeviceInput)
+          }
+        } catch {
+          log.info("\(#function): \(error.localizedDescription)")
+        }
+      }
+    }
+  }
+
+  func enableTorch() {
+    guard let delegate, let device = captureDeviceInput?.device, device.hasTorch else {
+      return
+    }
+
+    do {
+      try device.lockForConfiguration()
+      defer { device.unlockForConfiguration() }
+      if device.isTorchModeSupported(.on) {
+        device.torchMode = delegate.torchEnabled ? .on : .off
+      }
+    } catch {
+      log.info("\(#function): \(error.localizedDescription)")
+    }
+  }
+
+  func setFocusMode() {
+    guard let device = captureDeviceInput?.device, let delegate else {
+      return
+    }
+
+    do {
+      try device.lockForConfiguration()
+      defer { device.unlockForConfiguration() }
+      if device.isFocusModeSupported(delegate.autoFocus), device.focusMode != delegate.autoFocus {
+        device.focusMode = delegate.autoFocus
+      }
+    } catch {
+      log.info("\(#function): \(error.localizedDescription)")
+    }
+  }
+
+  func updateZoom() {
+    guard let device = captureDeviceInput?.device, let delegate else {
+      return
+    }
+
+    do {
+      try device.lockForConfiguration()
+      defer { device.unlockForConfiguration() }
+      let minZoom = 1.0
+      device.videoZoomFactor = minZoom * pow(device.activeFormat.videoMaxZoomFactor / minZoom, delegate.zoom)
+    } catch {
+      log.info("\(#function): \(error.localizedDescription)")
+    }
+  }
+
+  func getAvailableLenses() -> [String] {
+    guard let delegate else {
+      return []
+    }
+
+    let availableLenses = delegate.presetCamera == AVCaptureDevice.Position.back
+    ? deviceDiscovery.backCameraLenses
+    : deviceDiscovery.frontCameraLenses
+
+    // Lens ordering can be varied which causes problems if you keep the result in react state.
+    // We sort them to provide a stable ordering
+    return availableLenses.map { $0.localizedName }.sorted {
+      $0 < $1
+    }
+  }
+
+  func setupMovieFileCapture(withSessionConfiguration: Bool = true) {
+    let output = AVCaptureMovieFileOutput()
+    if withSessionConfiguration {
+      session.beginConfiguration()
+    }
+    defer {
+      if withSessionConfiguration {
+        session.commitConfiguration()
+      }
+    }
+    if session.canAddOutput(output) {
+      session.addOutput(output)
+      videoFileOutput = output
+    }
+  }
+
+  func cleanupMovieFileCapture(withSessionConfiguration: Bool = true) {
+    guard let videoFileOutput else {
+      return
+    }
+    if withSessionConfiguration {
+      session.beginConfiguration()
+    }
+    defer {
+      if withSessionConfiguration {
+        session.commitConfiguration()
+      }
+    }
+    if session.outputs.contains(videoFileOutput) {
+      session.removeOutput(videoFileOutput)
+      self.videoFileOutput = nil
+    }
+  }
+
+  func stopSession() {
+    guard hasAvailableCameraDevice else {
+      return
+    }
+
+    runtimeErrorTask?.cancel()
+    runtimeErrorTask = nil
+    session.beginConfiguration()
+    for input in self.session.inputs {
+      session.removeInput(input)
+    }
+
+    for output in session.outputs {
+      session.removeOutput(output)
+    }
+    session.commitConfiguration()
+
+    if session.isRunning {
+      session.stopRunning()
+    }
+  }
+
+  func addErrorNotification() {
+    guard let delegate else {
+      return
+    }
+
+    runtimeErrorTask?.cancel()
+    runtimeErrorTask = Task {
+      let errors = NotificationCenter.default.notifications(named: .AVCaptureSessionRuntimeError, object: self.session)
+        .compactMap({ $0.userInfo?[AVCaptureSessionErrorKey] as? AVError })
+      for await error in errors where error.code == .mediaServicesWereReset {
+        delegate.sessionQueue.async {
+          if !self.session.isRunning {
+            self.session.startRunning()
+          }
+          self.updateSessionAudioIsMuted()
+        }
+        delegate.onMountError(["message": "Camera session was reset"])
+      }
+    }
+  }
+
+  deinit {
+    runtimeErrorTask?.cancel()
+  }
+
+  var currentPhotoOutput: AVCapturePhotoOutput? { photoOutput }
+
+  var currentVideoFileOutput: AVCaptureMovieFileOutput? { videoFileOutput }
+
+  var currentDevice: AVCaptureDevice? { captureDeviceInput?.device }
+
+  private func addDevice(_ device: AVCaptureDevice) {
+    guard let delegate else {
+      return
+    }
+
+    if captureDeviceInput?.device.uniqueID == device.uniqueID {
+      return
+    }
+
+    session.beginConfiguration()
+    defer {
+      session.commitConfiguration()
+      delegate.emitAvailableLenses()
+      delegate.configurePreviewRotation()
+    }
+    if let captureDeviceInput {
+      session.removeInput(captureDeviceInput)
+    }
+
+    do {
+      let deviceInput = try AVCaptureDeviceInput(device: device)
+      if session.canAddInput(deviceInput) {
+        session.addInput(deviceInput)
+        captureDeviceInput = deviceInput
+        updateZoom()
+      }
+    } catch {
+      delegate.onMountError(["message": "Camera could not be started - \(error.localizedDescription)"])
+    }
+  }
+
+  func deviceDiscovery(_ discovery: DeviceDiscovery, didUpdateDevices devices: [AVCaptureDevice]) {
+    DispatchQueue.main.async { [weak self] in
+      self?.delegate?.emitAvailableLenses()
+    }
+  }
+
+  private var hasAvailableCameraDevice: Bool {
+    return AVCaptureDevice.default(for: .video) != nil
+  }
+
+  private func configureResponsiveCapture(_ photoOutput: AVCapturePhotoOutput) {
+    guard #available(iOS 17.0, *), photoOutput.isResponsiveCaptureSupported else {
+      return
+    }
+    photoOutput.isResponsiveCaptureEnabled = true
+    if photoOutput.isFastCapturePrioritizationSupported {
+      photoOutput.isFastCapturePrioritizationEnabled = true
+    }
+  }
+
+  private func startSession() {
+    guard hasAvailableCameraDevice else {
+      return
+    }
+
+    guard let delegate else {
+      return
+    }
+    guard let manager = delegate.permissionsManager else {
+      log.info("Permissions module not found.")
+      return
+    }
+    if !manager.hasGrantedPermission(usingRequesterClass: CameraOnlyPermissionRequester.self) {
+      delegate.onMountError(["message": "Camera permissions not granted - component could not be rendered."])
+      return
+    }
+
+    let photoOutput = AVCapturePhotoOutput()
+    photoOutput.isLivePhotoCaptureEnabled = false
+    session.beginConfiguration()
+    if session.canAddOutput(photoOutput) {
+      session.addOutput(photoOutput)
+      self.photoOutput = photoOutput
+    }
+
+    let preset = delegate.mode == .video
+    ? delegate.videoQuality.toPreset()
+    : delegate.pictureSize.toCapturePreset()
+    updateSessionPreset(preset: preset, withSessionConfiguration: false)
+
+    configureResponsiveCapture(photoOutput)
+
+    session.commitConfiguration()
+    addErrorNotification()
+    delegate.configurePreviewRotation()
+    delegate.barcodeScanner?.maybeStartBarcodeScanning()
+    enableTorch()
+  }
+}
diff --git a/node_modules/expo-camera/ios/Current/CameraVideoRecording.swift b/node_modules/expo-camera/ios/Current/CameraVideoRecording.swift
index 4db86b1..222fdfa 100644
--- a/node_modules/expo-camera/ios/Current/CameraVideoRecording.swift
+++ b/node_modules/expo-camera/ios/Current/CameraVideoRecording.swift
@@ -9,6 +9,8 @@ protocol CameraVideoRecordingDelegate: AnyObject {
   var appContext: AppContext? { get }
   var videoBitrate: Int? { get }
   var videoStabilizationMode: VideoStabilizationMode { get }
+
+  func prepareTorchForVideoRecording(reason: String) async
 }
 
 class CameraVideoRecording: NSObject, AVCaptureFileOutputRecordingDelegate {
@@ -23,27 +25,49 @@ class CameraVideoRecording: NSObject, AVCaptureFileOutputRecordingDelegate {
     super.init()
   }
 
+  private func logVideoRecordingDiagnostic(_ event: String, reason: String = "nil", videoFileOutput: AVCaptureMovieFileOutput? = nil) {
+    #if DEBUG
+    let output = videoFileOutput
+    let isRecording = output?.isRecording ?? false
+    let hasPromise = videoRecordedPromise != nil
+    let codec = videoCodecType?.rawValue ?? "nil"
+    let message = "[ExpoCameraTorch] event=\(event) reason=\(reason) t=\(CFAbsoluteTimeGetCurrent()) movieIsRecording=\(isRecording) hasPromise=\(hasPromise) codec=\(codec) validOptions=\(isValidVideoOptions)"
+    NSLog("%@", message)
+    #endif
+  }
+
   func record(options: CameraRecordingOptions, videoFileOutput: AVCaptureMovieFileOutput, promise: Promise) async {
     guard !videoFileOutput.isRecording && videoRecordedPromise == nil else {
+      logVideoRecordingDiagnostic("movie-record-skip-already-recording", videoFileOutput: videoFileOutput)
       return
     }
 
+    logVideoRecordingDiagnostic("movie-record-begin", videoFileOutput: videoFileOutput)
     if let connection = videoFileOutput.connection(with: .video) {
       let orientation = await delegate?.responsiveWhenOrientationLocked == true ?
         delegate?.physicalOrientation ?? .unknown : UIDevice.current.orientation
       connection.videoOrientation = ExpoCameraUtils.videoOrientation(for: orientation)
+      logVideoRecordingDiagnostic(
+        "movie-record-connection-configured",
+        reason: "orientation=\(connection.videoOrientation.rawValue) mirrored=\(connection.isVideoMirrored) stabilization=\(connection.preferredVideoStabilizationMode.rawValue)",
+        videoFileOutput: videoFileOutput
+      )
       await setVideoOptions(options: options, for: connection, videoFileOutput: videoFileOutput, promise: promise)
 
       if connection.isVideoOrientationSupported && delegate?.mirror == true {
         connection.isVideoMirrored = delegate?.mirror ?? false
       }
+    } else {
+      logVideoRecordingDiagnostic("movie-record-no-video-connection", videoFileOutput: videoFileOutput)
     }
 
     if !isValidVideoOptions {
+      logVideoRecordingDiagnostic("movie-record-invalid-options", videoFileOutput: videoFileOutput)
       return
     }
 
     guard let appContext = delegate?.appContext else {
+      logVideoRecordingDiagnostic("movie-record-no-app-context", videoFileOutput: videoFileOutput)
       promise.reject(CameraRecordingFailedException())
       return
     }
@@ -52,7 +76,15 @@ class CameraVideoRecording: NSObject, AVCaptureFileOutputRecordingDelegate {
     let fileUrl = URL(fileURLWithPath: path)
     videoRecordedPromise = promise
 
+    logVideoRecordingDiagnostic("movie-startRecording-before", reason: "url=\(fileUrl.lastPathComponent)", videoFileOutput: videoFileOutput)
     videoFileOutput.startRecording(to: fileUrl, recordingDelegate: self)
+    logVideoRecordingDiagnostic("movie-startRecording-after-call", videoFileOutput: videoFileOutput)
+    logVideoRecordingDiagnostic("movie-after-startRecording-sleep-begin", reason: "sleepMs=120", videoFileOutput: videoFileOutput)
+    try? await Task.sleep(nanoseconds: 120_000_000)
+    logVideoRecordingDiagnostic("movie-after-startRecording-sleep-end", videoFileOutput: videoFileOutput)
+    logVideoRecordingDiagnostic("movie-prepareTorch-after-startRecording-before", videoFileOutput: videoFileOutput)
+    await delegate?.prepareTorchForVideoRecording(reason: "after-startRecording")
+    logVideoRecordingDiagnostic("movie-prepareTorch-after-startRecording-after", videoFileOutput: videoFileOutput)
   }
 
   @available(iOS 18.0, *)
@@ -65,6 +97,7 @@ class CameraVideoRecording: NSObject, AVCaptureFileOutputRecordingDelegate {
   }
 
   func stopRecording(videoFileOutput: AVCaptureMovieFileOutput?) {
+    logVideoRecordingDiagnostic("movie-stopRecording", videoFileOutput: videoFileOutput)
     videoFileOutput?.stopRecording()
   }
 
@@ -75,18 +108,22 @@ class CameraVideoRecording: NSObject, AVCaptureFileOutputRecordingDelegate {
     promise: Promise
   ) async {
     isValidVideoOptions = true
+    logVideoRecordingDiagnostic("movie-setVideoOptions-begin", videoFileOutput: videoFileOutput)
 
     if let maxDuration = options.maxDuration {
       videoFileOutput.maxRecordedDuration = CMTime(seconds: maxDuration, preferredTimescale: 1000)
+      logVideoRecordingDiagnostic("movie-setVideoOptions-maxDuration", reason: "maxDuration=\(maxDuration)", videoFileOutput: videoFileOutput)
     }
 
     if let maxFileSize = options.maxFileSize {
       videoFileOutput.maxRecordedFileSize = Int64(maxFileSize)
+      logVideoRecordingDiagnostic("movie-setVideoOptions-maxFileSize", reason: "maxFileSize=\(maxFileSize)", videoFileOutput: videoFileOutput)
     }
 
     let avMode = (delegate?.videoStabilizationMode ?? .auto).toAVCaptureVideoStabilizationMode()
     if connection.isVideoStabilizationSupported {
       connection.preferredVideoStabilizationMode = avMode
+      logVideoRecordingDiagnostic("movie-setVideoOptions-stabilization", reason: "mode=\(avMode.rawValue)", videoFileOutput: videoFileOutput)
     }
 
     if let codec = options.codec {
@@ -98,7 +135,9 @@ class CameraVideoRecording: NSObject, AVCaptureFileOutputRecordingDelegate {
         }
         videoFileOutput.setOutputSettings(outputSettings, for: connection)
         self.videoCodecType = codecType
+        logVideoRecordingDiagnostic("movie-setVideoOptions-codec", reason: "codec=\(codecType.rawValue)", videoFileOutput: videoFileOutput)
       } else {
+        logVideoRecordingDiagnostic("movie-setVideoOptions-codec-rejected", reason: "codec=\(String(describing: options.codec?.rawValue))", videoFileOutput: videoFileOutput)
         promise.reject(CameraRecordingException(options.codec?.rawValue))
         videoRecordedPromise = nil
         isValidVideoOptions = false
@@ -106,12 +145,29 @@ class CameraVideoRecording: NSObject, AVCaptureFileOutputRecordingDelegate {
     }
   }
 
+  func fileOutput(
+    _ output: AVCaptureFileOutput,
+    didStartRecordingTo fileURL: URL,
+    from connections: [AVCaptureConnection]
+  ) {
+    logVideoRecordingDiagnostic(
+      "movie-didStartRecording",
+      reason: "url=\(fileURL.lastPathComponent) connections=\(connections.count)",
+      videoFileOutput: output as? AVCaptureMovieFileOutput
+    )
+  }
+
   func fileOutput(
     _ output: AVCaptureFileOutput,
     didFinishRecordingTo outputFileURL: URL,
     from connections: [AVCaptureConnection],
     error: Error?
   ) {
+    logVideoRecordingDiagnostic(
+      "movie-didFinishRecording",
+      reason: "error=\(error?.localizedDescription ?? "nil") url=\(outputFileURL.lastPathComponent)",
+      videoFileOutput: output as? AVCaptureMovieFileOutput
+    )
     defer {
       videoRecordedPromise = nil
       videoCodecType = nil
@@ -128,6 +184,7 @@ class CameraVideoRecording: NSObject, AVCaptureFileOutputRecordingDelegate {
   }
 
   func cleanup() {
+    logVideoRecordingDiagnostic("movie-cleanup")
     videoRecordedPromise?.reject(CameraUnmountedException())
     videoRecordedPromise = nil
     videoCodecType = nil
diff --git a/node_modules/expo-camera/ios/Current/CameraView.swift b/node_modules/expo-camera/ios/Current/CameraView.swift
index 7bf1b37..9475a7b 100644
--- a/node_modules/expo-camera/ios/Current/CameraView.swift
+++ b/node_modules/expo-camera/ios/Current/CameraView.swift
@@ -75,17 +75,29 @@ public class CameraView: ExpoView, EXAppLifecycleListener, EXCameraInterface, Ca
   var presetCamera = AVCaptureDevice.Position.back {
     didSet {
       sessionQueue.async {
+        self.sessionManager.logVideoTorchIntent(reason: "presetCamera-didSet preset=\(self.presetCamera.rawValue)")
         self.sessionManager.updateDevice()
       }
     }
   }
 
-  var flashMode = FlashMode.auto
+  var flashMode = FlashMode.auto {
+    didSet {
+      sessionQueue.async {
+        self.sessionManager.logVideoTorchIntent(reason: "flashMode-didSet value=\(self.flashMode.rawValue)")
+      }
+    }
+  }
 
   var torchEnabled = false {
     didSet {
       sessionQueue.async {
-        self.sessionManager.enableTorch()
+        self.sessionManager.logVideoTorchIntent(reason: "torchEnabled-didSet value=\(self.torchEnabled) mode=\(self.mode.rawValue)")
+        if self.torchEnabled {
+          self.sessionManager.enableTorchForVideoRecording(reason: "prop-video-intent")
+          return
+        }
+        self.sessionManager.enableTorch(reason: "prop-disable")
       }
     }
   }
@@ -107,6 +119,7 @@ public class CameraView: ExpoView, EXAppLifecycleListener, EXCameraInterface, Ca
   var mode = CameraMode.picture {
     didSet {
       sessionQueue.async {
+        self.sessionManager.logVideoTorchIntent(reason: "mode-didSet value=\(self.mode.rawValue) torchEnabled=\(self.torchEnabled)")
         self.sessionManager.setCameraMode()
       }
     }
@@ -123,6 +136,7 @@ public class CameraView: ExpoView, EXAppLifecycleListener, EXCameraInterface, Ca
   var active = true {
     didSet {
       sessionQueue.async {
+        self.sessionManager.logVideoTorchIntent(reason: "active-didSet value=\(self.active)")
         self.sessionManager.updateCameraIsActive()
       }
     }
@@ -211,10 +225,11 @@ public class CameraView: ExpoView, EXAppLifecycleListener, EXCameraInterface, Ca
       guard let self else {
         return
       }
+      sessionManager.logVideoTorchIntent(reason: "app-foregrounded paused=\(isSessionPaused)")
       if !session.isRunning && isSessionPaused {
         isSessionPaused = false
         session.startRunning()
-        if torchEnabled {
+        if torchEnabled && mode != .video {
           sessionManager.enableTorch()
         }
       }
@@ -226,6 +241,7 @@ public class CameraView: ExpoView, EXAppLifecycleListener, EXCameraInterface, Ca
       guard let self else {
         return
       }
+      sessionManager.logVideoTorchIntent(reason: "app-backgrounded paused=\(isSessionPaused)")
       if session.isRunning && !isSessionPaused {
         isSessionPaused = true
         session.stopRunning()
@@ -301,11 +317,45 @@ public class CameraView: ExpoView, EXAppLifecycleListener, EXCameraInterface, Ca
   }
 
   func record(options: CameraRecordingOptions, promise: Promise) async {
+    await withCheckedContinuation { (continuation: CheckedContinuation<Void, Never>) in
+      sessionQueue.async { [weak self] in
+        self?.sessionManager.logVideoTorchIntent(reason: "record-begin torchEnabled=\(self?.torchEnabled ?? false) mode=\(self?.mode.rawValue ?? "nil")")
+        continuation.resume()
+      }
+    }
     guard let videoFileOutput = sessionManager.currentVideoFileOutput else {
+      await withCheckedContinuation { (continuation: CheckedContinuation<Void, Never>) in
+        sessionQueue.async { [weak self] in
+          self?.sessionManager.logVideoTorchIntent(reason: "record-no-video-output")
+          continuation.resume()
+        }
+      }
       promise.reject(CameraOutputNotReadyException())
       return
     }
+    await withCheckedContinuation { (continuation: CheckedContinuation<Void, Never>) in
+      sessionQueue.async { [weak self] in
+        self?.sessionManager.logVideoTorchIntent(reason: "record-before-videoRecording-record isRecording=\(videoFileOutput.isRecording)")
+        self?.sessionManager.enableTorchForVideoRecording(reason: "record-before-startRecording")
+        continuation.resume()
+      }
+    }
     await videoRecording.record(options: options, videoFileOutput: videoFileOutput, promise: promise)
+    await withCheckedContinuation { (continuation: CheckedContinuation<Void, Never>) in
+      sessionQueue.async { [weak self] in
+        self?.sessionManager.logVideoTorchIntent(reason: "record-after-videoRecording-record isRecording=\(videoFileOutput.isRecording)")
+        continuation.resume()
+      }
+    }
+  }
+
+  func prepareTorchForVideoRecording(reason: String) async {
+    await withCheckedContinuation { (continuation: CheckedContinuation<Void, Never>) in
+      sessionQueue.async { [weak self] in
+        self?.sessionManager.enableTorchForVideoRecording(reason: reason)
+        continuation.resume()
+      }
+    }
   }
 
   @available(iOS 18.0, *)
@@ -339,6 +389,9 @@ public class CameraView: ExpoView, EXAppLifecycleListener, EXCameraInterface, Ca
   }
 
   func stopRecording() {
+    sessionQueue.async { [weak self] in
+      self?.sessionManager.logVideoTorchIntent(reason: "stopRecording-called")
+    }
     videoRecording.stopRecording(videoFileOutput: sessionManager.currentVideoFileOutput)
   }
 
diff --git a/node_modules/expo-camera/ios/Current/CameraView.swift.orig b/node_modules/expo-camera/ios/Current/CameraView.swift.orig
new file mode 100644
index 0000000..7bf1b37
--- /dev/null
+++ b/node_modules/expo-camera/ios/Current/CameraView.swift.orig
@@ -0,0 +1,438 @@
+import UIKit
+@preconcurrency import ExpoModulesCore
+import CoreMotion
+
+public class CameraView: ExpoView, EXAppLifecycleListener, EXCameraInterface, CameraEvent,
+  CameraSessionManagerDelegate, CameraPhotoCaptureDelegate, CameraVideoRecordingDelegate {
+  public var sessionQueue = DispatchQueue(label: "captureSessionQueue")
+  // Needed to satisfy EXCameraInterface
+  // swiftlint:disable implicitly_unwrapped_optional
+  public var session: AVCaptureSession!
+  private var sessionManager: CameraSessionManager!
+  private var photoCapture: CameraPhotoCapture!
+  private var videoRecording: CameraVideoRecording!
+  // swiftlint:enable implicitly_unwrapped_optional
+
+  private var lifecycleManager: EXAppLifecycleService?
+  internal var permissionsManager: EXPermissionsInterface?
+
+  internal var barcodeScanner: BarcodeScanner?
+
+  public override class var layerClass: AnyClass {
+    AVCaptureVideoPreviewLayer.self
+  }
+
+  internal var previewLayer: AVCaptureVideoPreviewLayer {
+    // swiftlint:disable:next force_cast
+    layer as! AVCaptureVideoPreviewLayer
+  }
+
+  internal var physicalOrientation: UIDeviceOrientation = .unknown
+  // Typed as Any? because AVCaptureDevice.RotationCoordinator is iOS 17+.
+  private var rotationCoordinator: Any?
+  private var previewRotationObservation: NSKeyValueObservation?
+  private var didStartSession = false
+  private var motionManager: CMMotionManager = {
+    let mm = CMMotionManager()
+    mm.accelerometerUpdateInterval = 0.2
+    mm.gyroUpdateInterval = 0.2
+    return mm
+  }()
+  private var isSessionPaused = false
+
+  // MARK: Property Observers
+
+  var responsiveWhenOrientationLocked = false {
+    didSet {
+      updateResponsiveOrientation()
+    }
+  }
+
+  var videoQuality: VideoQuality = .video1080p {
+    didSet {
+      sessionQueue.async {
+        if self.mode == .video {
+          self.sessionManager.updateSessionPreset(preset: self.videoQuality.toPreset())
+        }
+      }
+    }
+  }
+
+  var isScanningBarcodes = false {
+    didSet {
+      sessionQueue.async { [weak self] in
+        guard let self else {
+          return
+        }
+        barcodeScanner?.setIsEnabled(isScanningBarcodes)
+      }
+    }
+  }
+
+  internal var videoBitrate: Int?
+  var videoStabilizationMode: VideoStabilizationMode = .auto
+
+  var presetCamera = AVCaptureDevice.Position.back {
+    didSet {
+      sessionQueue.async {
+        self.sessionManager.updateDevice()
+      }
+    }
+  }
+
+  var flashMode = FlashMode.auto
+
+  var torchEnabled = false {
+    didSet {
+      sessionQueue.async {
+        self.sessionManager.enableTorch()
+      }
+    }
+  }
+
+  var autoFocus = AVCaptureDevice.FocusMode.continuousAutoFocus {
+    didSet {
+      sessionQueue.async {
+        self.sessionManager.setFocusMode()
+      }
+    }
+  }
+
+  var pictureSize = PictureSize.photo {
+    didSet {
+      updatePictureSize()
+    }
+  }
+
+  var mode = CameraMode.picture {
+    didSet {
+      sessionQueue.async {
+        self.sessionManager.setCameraMode()
+      }
+    }
+  }
+
+  var isMuted = false {
+    didSet {
+      sessionQueue.async {
+        self.sessionManager.updateSessionAudioIsMuted()
+      }
+    }
+  }
+
+  var active = true {
+    didSet {
+      sessionQueue.async {
+        self.sessionManager.updateCameraIsActive()
+      }
+    }
+  }
+
+  var selectedLens: String? {
+    didSet {
+      sessionQueue.async {
+        self.sessionManager.updateDevice()
+      }
+    }
+  }
+
+  var animateShutter = true
+  var mirror = false
+
+  var zoom: CGFloat = 0 {
+    didSet {
+      sessionQueue.async {
+        self.sessionManager.updateZoom()
+      }
+    }
+  }
+
+  // MARK: - Events
+
+  let onCameraReady = EventDispatcher()
+  let onMountError = EventDispatcher()
+  let onPictureSaved = EventDispatcher()
+  let onBarcodeScanned = EventDispatcher()
+  let onResponsiveOrientationChanged = EventDispatcher()
+  let onAvailableLensesChanged = EventDispatcher()
+
+  internal var deviceOrientation: UIInterfaceOrientation {
+    UIApplication.shared.connectedScenes.compactMap {
+      $0 as? UIWindowScene
+    }.first?.interfaceOrientation ?? .unknown
+  }
+
+  required init(appContext: AppContext? = nil) {
+    super.init(appContext: appContext)
+    lifecycleManager = appContext?.legacyModule(implementing: EXAppLifecycleService.self)
+    permissionsManager = appContext?.permissions
+
+    sessionManager = CameraSessionManager(delegate: self)
+    photoCapture = CameraPhotoCapture(delegate: self)
+    videoRecording = CameraVideoRecording(delegate: self)
+    session = sessionManager.session
+
+    setupPreview()
+    barcodeScanner = createBarcodeScanner()
+    UIDevice.current.beginGeneratingDeviceOrientationNotifications()
+    NotificationCenter.default.addObserver(
+      self,
+      selector: #selector(orientationChanged),
+      name: UIDevice.orientationDidChangeNotification,
+      object: nil)
+    lifecycleManager?.register(self)
+    sessionManager.initializeCaptureSessionInput()
+  }
+
+  private func setupPreview() {
+    previewLayer.session = sessionManager.session
+    previewLayer.videoGravity = .resizeAspectFill
+    previewLayer.needsDisplayOnBoundsChange = true
+  }
+
+  func startSessionIfNeeded() {
+    guard !didStartSession else {
+      return
+    }
+    didStartSession = true
+    sessionQueue.async { [weak self] in
+      guard let self else {
+        return
+      }
+      self.sessionManager.updateCameraIsActive()
+      DispatchQueue.main.async {
+        self.onCameraReady()
+      }
+    }
+  }
+
+  public func onAppForegrounded() {
+    sessionQueue.async { [weak self] in
+      guard let self else {
+        return
+      }
+      if !session.isRunning && isSessionPaused {
+        isSessionPaused = false
+        session.startRunning()
+        if torchEnabled {
+          sessionManager.enableTorch()
+        }
+      }
+    }
+  }
+
+  public func onAppBackgrounded() {
+    sessionQueue.async { [weak self] in
+      guard let self else {
+        return
+      }
+      if session.isRunning && !isSessionPaused {
+        isSessionPaused = true
+        session.stopRunning()
+      }
+    }
+  }
+
+  private func updatePictureSize() {
+    sessionQueue.async {
+      if self.mode == .picture {
+        let preset = self.pictureSize.toCapturePreset()
+        self.sessionManager.updateSessionPreset(preset: preset)
+      }
+    }
+  }
+
+  func setBarcodeScannerSettings(settings: BarcodeSettings) {
+    sessionQueue.async {
+      self.barcodeScanner?.setSettings([BARCODE_TYPES_KEY: settings.toMetadataObjectType()])
+    }
+  }
+
+  func emitAvailableLenses() {
+    DispatchQueue.main.async { [weak self] in
+      guard let self else {
+        return
+      }
+      self.onAvailableLensesChanged([
+        "lenses": self.sessionManager.getAvailableLenses()
+      ])
+    }
+  }
+
+  func getAvailableLenses() -> [String] {
+    return sessionManager.getAvailableLenses()
+  }
+
+  func updateResponsiveOrientation() {
+    if responsiveWhenOrientationLocked {
+      motionManager.startAccelerometerUpdates(to: OperationQueue()) { [weak self] _, error in
+        if error != nil {
+          return
+        }
+        guard let self, let accelerometerData = self.motionManager.accelerometerData else {
+          return
+        }
+
+        let deviceOrientation = ExpoCameraUtils.deviceOrientation(
+          for: accelerometerData,
+          default: self.physicalOrientation)
+        if deviceOrientation != self.physicalOrientation {
+          self.physicalOrientation = deviceOrientation
+          self.onResponsiveOrientationChanged(["orientation": ExpoCameraUtils.toOrientationString(orientation: deviceOrientation)])
+        }
+      }
+    } else {
+      motionManager.stopAccelerometerUpdates()
+    }
+  }
+
+  func takePictureRef(options: TakePictureOptions) async throws -> PictureRef {
+    guard let photoOutput = sessionManager.currentPhotoOutput else {
+      throw CameraOutputNotReadyException()
+    }
+    return try await photoCapture.takePictureRef(options: options, photoOutput: photoOutput)
+  }
+
+  func takePicturePromise(options: TakePictureOptions) async throws -> [String: Any] {
+    guard let photoOutput = sessionManager.currentPhotoOutput else {
+      throw CameraOutputNotReadyException()
+    }
+    return try await photoCapture.takePicturePromise(options: options, photoOutput: photoOutput)
+  }
+
+  func record(options: CameraRecordingOptions, promise: Promise) async {
+    guard let videoFileOutput = sessionManager.currentVideoFileOutput else {
+      promise.reject(CameraOutputNotReadyException())
+      return
+    }
+    await videoRecording.record(options: options, videoFileOutput: videoFileOutput, promise: promise)
+  }
+
+  @available(iOS 18.0, *)
+  func toggleRecording() {
+    guard let videoFileOutput = sessionManager.currentVideoFileOutput else {
+      return
+    }
+    videoRecording.toggleRecording(videoFileOutput: videoFileOutput)
+  }
+
+  public override func didMoveToWindow() {
+    super.didMoveToWindow()
+    if window != nil {
+      configurePreviewRotation()
+      startSessionIfNeeded()
+    }
+  }
+
+  public override func removeFromSuperview() {
+    super.removeFromSuperview()
+    sessionQueue.async { [weak self] in
+      self?.sessionManager.stopSession()
+    }
+    motionManager.stopAccelerometerUpdates()
+    lifecycleManager?.unregisterAppLifecycleListener(self)
+    UIDevice.current.endGeneratingDeviceOrientationNotifications()
+    NotificationCenter.default.removeObserver(self, name: UIDevice.orientationDidChangeNotification, object: nil)
+    previewRotationObservation?.invalidate()
+    previewRotationObservation = nil
+    rotationCoordinator = nil
+  }
+
+  func stopRecording() {
+    videoRecording.stopRecording(videoFileOutput: sessionManager.currentVideoFileOutput)
+  }
+
+  func resumePreview() {
+    previewLayer.connection?.isEnabled = true
+  }
+
+  func pausePreview() {
+    previewLayer.connection?.isEnabled = false
+  }
+
+  @objc func orientationChanged() {
+    // On iOS 17+ the RotationCoordinator's KVO drives preview rotation; the legacy
+    // notification path is only needed on iOS 16.
+    if #available(iOS 17.0, *) {
+      return
+    }
+    applyLegacyPreviewOrientation()
+  }
+
+  func configurePreviewRotation() {
+    if #available(iOS 17.0, *) {
+      Task { @MainActor in
+        self.setUpRotationCoordinator()
+      }
+    } else {
+      applyLegacyPreviewOrientation()
+    }
+  }
+
+  @available(iOS 17.0, *)
+  private func setUpRotationCoordinator() {
+    previewRotationObservation?.invalidate()
+    previewRotationObservation = nil
+
+    guard let device = sessionManager.currentDevice else {
+      rotationCoordinator = nil
+      return
+    }
+
+    let coordinator = AVCaptureDevice.RotationCoordinator(device: device, previewLayer: previewLayer)
+    rotationCoordinator = coordinator
+    applyPreviewRotationAngle(from: coordinator)
+
+    previewRotationObservation = coordinator.observe(
+      \.videoRotationAngleForHorizonLevelPreview,
+      options: [.new]
+    ) { [weak self] coordinator, _ in
+      self?.applyPreviewRotationAngle(from: coordinator)
+    }
+  }
+
+  @available(iOS 17.0, *)
+  private func applyPreviewRotationAngle(from coordinator: AVCaptureDevice.RotationCoordinator) {
+    guard let connection = previewLayer.connection else {
+      return
+    }
+    let angle = coordinator.videoRotationAngleForHorizonLevelPreview
+    if connection.isVideoRotationAngleSupported(angle) {
+      connection.videoRotationAngle = angle
+    }
+  }
+
+  private func applyLegacyPreviewOrientation() {
+    // We shouldn't access the device orientation anywhere but on the main thread
+    Task { @MainActor in
+      let videoOrientation = ExpoCameraUtils.videoOrientation(for: deviceOrientation)
+      if (previewLayer.connection?.isVideoOrientationSupported) == true {
+        physicalOrientation = ExpoCameraUtils.physicalOrientation(for: deviceOrientation)
+        previewLayer.connection?.videoOrientation = videoOrientation
+      }
+    }
+  }
+
+  private func createBarcodeScanner() -> BarcodeScanner {
+    let scanner = BarcodeScanner(session: session, sessionQueue: sessionQueue)
+
+    scanner.setPreviewLayer(layer: previewLayer)
+    scanner.setOnBarcodeScanned { [weak self] body in
+      guard let self else {
+        return
+      }
+      if let body {
+        self.onBarcodeScanned(body)
+      }
+    }
+
+    return scanner
+  }
+
+  deinit {
+    previewRotationObservation?.invalidate()
+    motionManager.stopAccelerometerUpdates()
+    photoCapture.cleanup()
+    videoRecording.cleanup()
+  }
+}
